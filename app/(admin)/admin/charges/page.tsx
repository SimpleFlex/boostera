import EmptyState from "../ui/EmptyState";

export default function ChargesPage() {
  return (
    <EmptyState
      title="Charges"
      desc="Next: admin can create charge requests, users approve in-wallet, and status updates to paid."
    />
  );
}
